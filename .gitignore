# Patient / large data — NEVER commit.
# Root-anchored (leading slash) so scanner/data/*.json IS still tracked.
# 575 real TCIA CTs + legacy fakes + baselines (not ours to redistribute):
/data/
# legacy RF classifier .pkl (parked deepfake era):
/models/

# Scan / disarm output artifacts
*.disarmed.dcm
*.cdrtest.dcm
*_report.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.venv/
venv/
.pytest_cache/

# Internal continuity / planning docs — keep local, NOT for the public repo
# (candid competitive notes, business strategy, founder/mentor + med-school context)
PROGRESS_LOG.md
SESSION_3_PLAN.md

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
