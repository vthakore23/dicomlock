# Patient / large data — NEVER commit.
# Root-anchored (leading slash) so scanner/data/*.json IS still tracked.
# 575 real TCIA CTs + legacy fakes + baselines (not ours to redistribute):
/data/
# legacy RF classifier .pkl (parked deepfake era):
/models/

# Scan / disarm output artifacts
*.disarmed.dcm
*.cdrtest.dcm
*_report.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.venv/
venv/
.pytest_cache/

# Internal continuity / planning docs — keep local, NOT for the public repo
# (candid competitive notes, business strategy, founder/mentor + med-school context)
PROGRESS_LOG.md
SESSION_3_PLAN.md
AUDIT.md
DIRECTION.md
NEXT_CHAT.md
PLAN.md

# Fuzzing run output + generated result artifacts (regenerated; some crash filenames
# contain ':' which is not portable to Windows checkouts). The harness + seeds are tracked.
_attack_test/aim3/results/
_attack_test/*_results.txt
# Benchmark engine output (regenerated by `python -m bench`)
bench/results.json
# Generated adversarial corpus (regenerated by `python -m bench.generate`)
samples/generated/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
.Rhistory
